# Dépendances de PRODUCTION : ce dont l'API a besoin pour servir des prédictions.
# C'est ce fichier qu'installe le Dockerfile — il ne doit contenir que le strict nécessaire.
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
joblib==1.6.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
